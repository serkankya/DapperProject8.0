@using Project.Shared.DTOs.VehicleDtos
@model List<ResultVehicleDto>

<section class="ftco-section bg-light">
	<div class="container">
		<div class="row">

			@foreach (var item in Model)
			{
				<div class="col-md-4">
					<div class="car-wrap rounded ftco-animate">
						<div class="img rounded d-flex align-items-end" style="background-image: url('@Url.Content("~/design/images/car-1.jpg")');">
						</div>
						<div class="text">
							<h2 class="mb-0"><a href="car-single.html">@item.BrandName @item.ModelName</a></h2>
							<div class="d-flex mb-3">
								<span class="cat">@item.CategoryName</span>
								<p class="price ml-auto">@item.PricePerDay ₺ <span>/gün</span></p>
							</div>
							<p class="d-flex mb-0 d-block"><a href="#" class="btn btn-primary py-2 mr-1">Şimdi kirala</a> <a href="@Url.Action("Details","Vehicles",new{id= item.VehicleId})" class="btn btn-secondary py-2 ml-1">Detaylar</a></p>
						</div>
					</div>
				</div>
			}

		</div>
	</div>
</section>